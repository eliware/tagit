# Command guide

Use `tagit notes`, `tagit preflight`, and `tagit push` as the project owner
handoff workflow. DevOps performs release and post-release verification after
preflight passes. See the [command specification](../specs/commands.md) for
normative behavior.

All commands run from the target repository root and report actionable errors.
