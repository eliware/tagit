import { getReleaseVersion, isHelp, parseOptions } from '../../../src/cli/arguments/parse-options.mjs';
test('parses supported commands and release versions', () => { expect(isHelp(['--help'])).toBe(true); expect(getReleaseVersion(['release', '--version', '1.2.3'])).toBe('1.2.3'); expect(parseOptions(['preflight', '--ignore-100x4', '--ignore-monolith-limits'])).toMatchObject({ command: 'preflight', ignore100x4: true, ignoreMonolithLimits: true }); expect(parseOptions(['--version'])).toMatchObject({ versionQuery: true }); });
test('rejects invalid and unauthorized combinations', () => { expect(() => parseOptions(['unknown'])).toThrow('Unknown command'); expect(() => parseOptions(['notes', '--bad'])).toThrow('Unknown option'); expect(() => parseOptions(['notes', 'extra'])).toThrow('Unexpected positional'); expect(() => parseOptions(['preflight', '--dry-run', '--dry-run'])).toThrow('Duplicate'); expect(() => parseOptions(['notes', '--version', '1.0.0'])).toThrow('--version requires'); expect(() => parseOptions(['push', '--ignore-100x4'])).toThrow('requires preflight'); expect(() => parseOptions(['release-wait', '--version', '1.0.0'])).toThrow('latest tag'); expect(() => getReleaseVersion(['release', '--version', 'bad'])).toThrow('Invalid'); expect(() => parseOptions(['release', '--version', 'bad'])).toThrow('Invalid'); expect(() => getReleaseVersion(['release', '--version', '01.2.3'])).toThrow('Invalid'); expect(getReleaseVersion(['release'])).toBeNull(); expect(getReleaseVersion(['release', '--version'])).toBeNull(); expect(getReleaseVersion(['release', '--version', '-x'])).toBeNull(); expect(() => parseOptions(['release', '--version', '1.0.0', '--version', '1.0.0'])).toThrow('Duplicate'); expect(() => parseOptions(['release', '--version', '1.0.0', 'extra'])).toThrow('Unexpected'); expect(() => parseOptions(['notes', '--dry-run'])).toThrow('requires push'); expect(() => parseOptions(['--dry-run', 'release'])).toThrow('command must precede'); expect(() => parseOptions(['release', '--version'])).toThrow('value is required'); expect(() => getReleaseVersion(['release'], { required: true })).toThrow('required'); expect(() => getReleaseVersion(['release', '--version'], { required: true })).toThrow('required'); expect(() => parseOptions(['--help', '--help'])).toThrow('Duplicate'); expect(() => parseOptions(['--help', '--version'])).toThrow('cannot be combined'); });
